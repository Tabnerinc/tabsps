
POST  /signup         com.h2o.ps360.controller.Ps360SignupPageController.SignupPage

POST /signin          com.h2o.ps360.controller.ManagePatientAccount.patientLogin

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file          controllers.Assets.versioned(path="/public", file: Asset)
